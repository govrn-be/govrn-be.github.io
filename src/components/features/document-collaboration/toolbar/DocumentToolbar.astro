---
interface Props {
  tools: Array<{
    icon: string;
    label: string;
  }>;
  texts: {
    version: string;
    share: string;
    shareThrough: string;
    shareOptions: string[];
  };
}

const { tools = [
  { icon: "✏️", label: "Annotate" },
  { icon: "💬", label: "Comment" },
  { icon: "🔍", label: "Search" },
  { icon: "🔄", label: "Version History" }
], texts = {
  version: "Version 1.2",
  share: "Share",
  shareThrough: "Share through:",
  shareOptions: ["📧 Email", "💬 Private Chat", "📱 Mobile App"]
}} = Astro.props;
---

<div class="bg-white border-b border-gray-100 p-4 relative overflow-visible">
  <div class="flex items-center justify-between">
    <div class="flex items-center space-x-2">
      {tools.map(tool => (
        <button
          class="p-2 rounded-lg hover:bg-gray-100 text-gray-600 hover:text-gray-900 transition-colors group relative z-10"
          title={tool.label}
        >
          <span class="text-xl">{tool.icon}</span>
          <span class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-2 py-1 text-xs font-medium text-white bg-gray-900 rounded opacity-0 group-hover:opacity-100 transition-opacity">
            {tool.label}
          </span>
        </button>
      ))}
    </div>

    <div class="flex items-center space-x-4">
      <span class="text-sm text-gray-500">{texts.version}</span>
      <button class="text-sm text-primary hover:text-primary/90 font-medium group relative">
        {texts.share}
        <div class="absolute bottom-full right-0 mb-2 w-48 bg-gray-900 rounded-lg py-2 px-3 text-xs text-white opacity-0 group-hover:opacity-100 transition-opacity z-50">
          <p class="mb-1">{texts.shareThrough}</p>
          <ul class="space-y-1">
            {texts.shareOptions.map(option => (
              <li>{option}</li>
            ))}
          </ul>
        </div>
      </button>
    </div>
  </div>
</div>