---
import Layout from '../layouts/Layout.astro';

const securityFeatures = [
  {
    icon: "🔒",
    title: "Enterprise-Grade Infrastructure",
    description: "Built on Microsoft Azure's secure cloud platform, benefiting from their comprehensive security measures"
  },
  {
    icon: "🛡️",
    title: "Data Protection",
    description: "Robust data protection practices aligned with GDPR requirements and industry standards"
  },
  {
    icon: "🔐",
    title: "Advanced Encryption",
    description: "End-to-end encryption for all data transmission and storage using industry-leading protocols"
  },
  {
    icon: "👤",
    title: "Granular Access Control",
    description: "Role-based access management with comprehensive audit trails"
  }
];

const securityLayers = [
  {
    title: "Infrastructure Security",
    icon: "🏢",
    description: "Leveraging Microsoft Azure's secure cloud infrastructure",
    features: [
      "High-availability data centers",
      "Network security controls",
      "DDoS protection",
      "Regular security updates"
    ]
  },
  {
    title: "Application Security",
    icon: "🔐",
    description: "Multiple layers of security at the application level",
    features: [
      "Two-factor authentication (2FA)",
      "Session management",
      "Input validation",
      "Security headers"
    ]
  },
  {
    title: "Data Protection",
    icon: "🛡️",
    description: "Comprehensive data security measures",
    features: [
      "End-to-end encryption",
      "Secure data storage",
      "Regular backups",
      "Data access controls"
    ]
  }
];

const trustedPartners = [
  {
    name: "Microsoft Azure",
    logo: "/azure-logo.png",
    description: "Our infrastructure provider, offering enterprise-grade security with certifications including ISO 27001, FedRAMP, SOC, and HIPAA compliance",
    features: [
      "Global data center network",
      "Advanced threat protection",
      "Continuous security monitoring",
      "Compliance frameworks"
    ]
  },
  {
    name: "UniverSign",
    logo: "/universign-logo.png",
    description: "Our electronic signature partner, compliant with eIDAS regulations",
    features: [
      "Qualified electronic signatures",
      "Legal compliance",
      "Secure document handling",
      "Audit trails"
    ]
  },
  {
    name: "Apryse",
    logo: "/apryse-logo.png",
    description: "Our document processing partner, offering secure document handling with multiple compliance certifications",
    features: [
      "Secure document processing",
      "Access control",
      "Encryption",
      "Compliance features"
    ]
  }
];
---

<Layout title="Security - Govrn Board Management Software">
  <main class="pt-24">
    <!-- Hero Section -->
    <section class="relative bg-gradient-to-br from-white to-gray-50 py-32 overflow-hidden">
      <div class="absolute inset-0 z-0">
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_right,rgba(57,153,222,0.1),transparent_50%)]"></div>
      </div>

      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="text-center max-w-3xl mx-auto">
          <div class="inline-flex items-center rounded-full px-4 py-1 text-sm font-medium bg-primary/10 text-primary mb-8">
            Enterprise-Grade Security
          </div>
          <h1 class="text-5xl font-extrabold text-gray-900 tracking-tight mb-8">
            Security at Every
            <span class="bg-clip-text text-transparent bg-gradient-to-r from-primary to-blue-600">
              Layer
            </span>
          </h1>
          <p class="text-xl text-gray-600 leading-relaxed">
            At Govrn, we've built security into every aspect of our platform, partnering with industry leaders to ensure your board's data is protected by the most robust security measures available.
          </p>
        </div>
      </div>
    </section>

    <!-- Security Features Grid -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-bold text-gray-900">Comprehensive Security</h2>
          <p class="mt-4 text-xl text-gray-600">Built-in security features that protect your board's sensitive information</p>
        </div>
        <div class="grid md:grid-cols-2 gap-8">
          {securityFeatures.map(feature => (
            <div class="bg-white rounded-xl p-8 shadow-lg border border-gray-100">
              <div class="text-4xl mb-6">{feature.icon}</div>
              <h3 class="text-xl font-bold text-gray-900 mb-4">{feature.title}</h3>
              <p class="text-gray-600">{feature.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Security Layers -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-bold text-gray-900">Multi-Layered Security</h2>
          <p class="mt-4 text-xl text-gray-600">Defense in depth approach to protect your data</p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
          {securityLayers.map(layer => (
            <div class="bg-white rounded-xl p-8 shadow-lg border border-gray-100">
              <div class="text-4xl mb-6">{layer.icon}</div>
              <h3 class="text-xl font-bold text-gray-900 mb-4">{layer.title}</h3>
              <p class="text-gray-600 mb-6">{layer.description}</p>
              <ul class="space-y-3">
                {layer.features.map(feature => (
                  <li class="flex items-center text-gray-600">
                    <svg class="h-5 w-5 text-primary mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    {feature}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Trusted Partners -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-bold text-gray-900">Our Security Partners</h2>
          <p class="mt-4 text-xl text-gray-600">Working with industry leaders to ensure the highest security standards</p>
        </div>

        <div class="space-y-12">
          {trustedPartners.map(partner => (
            <div class="bg-white rounded-xl p-8 shadow-lg border border-gray-100">
              <div class="flex flex-col md:flex-row gap-8">
                <div class="md:w-64 flex-shrink-0 flex items-center">
                  <img
                    src={partner.logo}
                    alt={`${partner.name} logo`}
                    class="h-12"
                  />
                </div>
                <div class="flex-grow">
                  <h3 class="text-xl font-bold text-gray-900 mb-4">{partner.name}</h3>
                  <p class="text-gray-600 mb-6">{partner.description}</p>
                  <div class="grid sm:grid-cols-2 gap-4">
                    {partner.features.map(feature => (
                      <div class="flex items-center text-gray-600">
                        <svg class="h-5 w-5 text-primary mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                        {feature}
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="bg-gradient-to-br from-primary to-blue-600 py-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl font-bold text-white mb-8">
          Ready to Experience Secure Board Management?
        </h2>
        <p class="text-xl text-white/90 mb-12 max-w-2xl mx-auto">
          Join leading organizations that trust Govrn with their sensitive board information
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/request-demo"
            class="inline-flex items-center px-8 py-4 text-lg font-medium rounded-xl text-primary bg-white hover:bg-gray-50 transition-colors duration-200"
          >
            Request Demo
          </a>
          <a
            href="/contact"
            class="inline-flex items-center px-8 py-4 text-lg font-medium rounded-xl text-white border-2 border-white hover:bg-white/10 transition-colors duration-200"
          >
            Contact Sales
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>