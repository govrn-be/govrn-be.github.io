---
interface Props {
  texts?: {
    title: string;
    subtitle: string;
    stats: Array<{
      value: string;
      label: string;
      icon: string;
    }>;
  };
}

const { texts = {
  title: "Measurable Impact",
  subtitle: "See the difference real-time collaboration makes",
  stats: [
    {
      value: "50%",
      label: "Faster Document Reviews",
      icon: "⚡"
    },
    {
      value: "100%", 
      label: "Real-Time Visibility",
      icon: "👁️"
    },
    {
      value: "30%",
      label: "More Board Engagement", 
      icon: "📈"
    },
    {
      value: "24/7",
      label: "Secure Access",
      icon: "🔒"
    }
  ]
}} = Astro.props;
---

<section class="py-20 bg-gradient-to-br from-primary to-blue-600 text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl font-bold mb-4">
        {texts.title}
      </h2>
      <p class="text-xl text-white/90">
        {texts.subtitle}
      </p>
    </div>

    <div class="grid md:grid-cols-4 gap-8">
      {texts.stats.map(stat => (
        <div class="relative group">
          <div class="absolute inset-0 bg-white/5 rounded-xl blur-xl group-hover:bg-white/10 transition-colors duration-300"></div>
          
          <div class="relative bg-white/10 backdrop-blur-sm rounded-xl p-8 border border-white/10">
            <div class="text-3xl mb-4">{stat.icon}</div>
            <div class="text-4xl font-bold mb-2">{stat.value}</div>
            <p class="text-white/80">{stat.label}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>